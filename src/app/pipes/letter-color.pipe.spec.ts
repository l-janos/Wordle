import { LetterColorPipe } from './letter-color.pipe';

describe('LetterColorPipe', () => {
  it('create an instance', () => {
    const pipe = new LetterColorPipe();
    expect(pipe).toBeTruthy();
  });
});
